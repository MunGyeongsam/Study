using System;
using System.Collections.Generic;
using System.Text;

namespace jungol.LanguageCSharp
{
    internal class _05_Operator
    {
        // +, -, *, /, %

        // =, +=, -=, *=, /=, %=

        // ++, --

        // &&, ||, !

        // <, >, ==, !=, <=, >=

        // &, |. ^(XOR)

        // >>, <<

        // ?:
        // ??
    }
}
