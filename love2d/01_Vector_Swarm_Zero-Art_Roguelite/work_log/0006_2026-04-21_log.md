# 0006_2026-04-21_log.md

## 작업 요약: 4B.2 튜토리얼 (첫 30초 가이드)

---

### 세션 1: 4B.2 Tutorial Hints 구현

#### 설계 결정
- **방향**: 첫 플레이 감지(`totalRuns == 0`) → 상황별 미니멀 힌트 순차 표시
- **B안 + 주목 연출 (하이브리드)**: PlayScene 내부 HUD 레이어 + 슬로모/딤/글리치 텍스트
- **순수 텍스트만 띄우는 B안 기각**: 탄막 게임 중 텍스트만으로는 시선 확보 불가
- **A안(Scene 오버레이) 기각**: transparent Scene에서 입력 전달 복잡 + 과잉 설계

#### 연출 설계
- Phase machine: `delay → entering → showing → completing → delay → ...`
- 슬로모 진입 (timeScale 0.15) → 화면 딤 (alpha 0.35) → 글리치 텍스트 타이핑
- 액션 성공 시 → 글리치 아웃 + 속도 복귀 (1.0)
- 기존 연출 재활용: gameState.timeScale (포커스 슬로모), 글리치 텍스트 (보스 처치), 딤 오버레이 (일시정지)

#### 힌트 시퀀스 (4단계)
| # | 트리거 | 힌트 | 완료 조건 |
|---|--------|------|----------|
| 1 | 게임 시작 0.5초 후 | `W A S D -- MOVE` | 이동 입력 감지 |
| 2 | Move 완료 1초 후 | `SHIFT -- DASH` | 대쉬 사용 (cooldownTimer > 0) |
| 3 | Dash 완료 1초 후 | `SPACE -- FOCUS (SLOW TIME)` | 포커스 활성 (focus.active) |
| 4 | Focus 완료 1초 후 | `WEAPON FIRES AUTOMATICALLY` | 3초 타이머 |

#### timeScale 충돌 해결
- tutorialHints.update()는 ecsManager.update() **이후** 실행
- 포커스 시스템이 timeScale을 설정해도 tutorial이 오버라이드
- 완료(completing) phase에서 timeScale → 1.0 복귀 시 포커스가 자연스럽게 인수

### 파일 변경 목록
**신규:**
- `src/03_game/states/tutorialHints.lua` — 튜토리얼 힌트 시스템 (4단계 + 연출)

**수정:**
- `src/00_common/saveData.lua` — `tutorialDone` 필드 + `setTutorialDone()` API 추가
- `src/03_game/scenes/playScene.lua` — tutorialHints require, init, update, draw 통합
- `doc/00_PROJECT_ROADMAP.md` — 4B.2 ✅ 체크

### 테스트 결과
- [x] 크래시 없이 실행
- [x] 첫 플레이 감지 정상 (totalRuns == 0)
- [x] Step 1~4 순차 진행 + 액션 감지
- [x] 슬로모 + 딤 + 글리치 텍스트 연출
- [x] 완료 후 저장 + 이후 게임 정상 진행
- [x] 스테이지 클리어, 레벨업, 일시정지 정상

---

### 다음 작업 (예정)
1. 4D.1 밸런싱 & QA — 전체 난이도 곡선 튜닝
2. Phase 4B 완료 확정 (4B.1 ✅, 4B.2 ✅, 4B.3 ✅)

---

## 세션 2: 4C.3 미니맵

### 설계 결정
- **위치**: Play 영역 우상단 오버레이 (topHud 아래)
- **크기**: 화면 너비의 18%, 월드 비율(2:3) 유지 → 해상도에 따라 자동 스케일
- **표시 대상**: 플레이어(흰 원), 적(빨간 점), 보스(큰 빨간 점), XP 오브(녹색 점), 카메라 뷰포트(파란 테두리)
- **Scissor clip**: 미니맵 영역 밖으로 점이 넘치지 않게

### 구현
- [x] `minimap.lua` 신규 모듈 (04_ui/)
- [x] `uiManager`에 minimap init/updateLayout/draw 통합
- [x] `playScene`에서 `uiManager.setMinimapData(ecs, player, cam)` 호출
- [x] 카메라 API: `cam:pos()` (기존 camera.lua)

### 파일 변경 목록
**신규:**
- `src/04_ui/minimap.lua` — 미니맵 모듈

**수정:**
- `src/04_ui/uiManager.lua` — minimap require + init + layout + draw + setMinimapData
- `src/03_game/scenes/playScene.lua` — setMinimapData 호출 추가
- `doc/00_PROJECT_ROADMAP.md` — 4C.3 ✅ 체크

### 테스트 결과
- [x] 크래시 없이 실행
- [x] 미니맵 우상단 표시
- [x] 플레이어/적/카메라 뷰포트 표시
- [x] 튜토리얼 + 미니맵 동시 동작
- [x] 일시정지/메인메뉴 복귀 정상

---

### 다음 작업 (예정)
1. 4D.1 밸런싱 & QA — 전체 난이도 곡선 튜닝
2. Phase 4C 완료 확정 (4C.1 ✅, 4C.2 ✅, 4C.3 ✅)

---

### 세션 3: 문서 업데이트 (4건)

#### A. doc/06 플레이 가이드 업데이트 ✅
- 화면 흐름도 (Scene Stack 기반 8개 씬) 추가
- 첫 플레이 튜토리얼 (슬로모+글리치 4단계) 섹션 추가
- 미니맵 설명 섹션 추가
- 메타 성장 (Data Fragment + 영구 강화 트리 + 도전과제) 섹션 추가
- 디버그 키 테이블 업데이트 (F6~F12 추가)
- 메뉴 조작키 (↑↓/Return/Space/ESC), U키(업그레이드) 추가

#### B. README.md 업데이트 ✅
- Phase 0 초안 → Phase 4 현재 상태 반영으로 전면 재작성
- 실제 프로젝트 구조 (src/ 번호 레이어 시스템) 반영
- 게임 특징: Core Loop / Meta Growth / Content / Zero-Art 4섹션
- 개발 상태 진행바 추가 (Phase 0~4)
- 구 README의 없는 파일 참조 (registry.lua, batcher.lua 등) 제거

#### C. doc/13 폴리싱 설계 갭 분석 ✅
- 구현 상태 헤더 추가 (4A✅ / 4B✅ / 4C✅ / 4D🔲 / 4E🔲)
- 약점 진단 4개 → 전부 해소로 업데이트 (strikethrough)
- 화면 흐름도 → 실제 구현 반영 (OPTIONS 삭제, ACHIEVEMENTS 추가, Scene Stack 연산 표시)
- 타이틀 메뉴 구성 → PLAY/UPGRADES/ACHIEVEMENTS/CREDITS
- 튜토리얼 → 설계(트리거 5단계) vs 구현(액션감지 4단계) 변경점 반영
- 미니맵 → "선택적" → 구현 완료 상세
- 4C.1/4C.2 "기술부채" 태그 → ✅ 마커

#### D. doc/14 씬스택 아키텍처 업데이트 ✅
- 상태: "설계 단계" → "구현 완료 (Phase 4B.1)"
- Scene 매핑 테이블 → 실제 8개 씬 반영 (OptionsScene/TutorialScene 삭제)
- 게임 플로우 → ACHIEVEMENTS/CREDITS 씬, U키 업그레이드, auto-pop 반영
- 마이그레이션 6단계 → 전부 ✅ 완료
- gameState 역할 변화 → 완료 표시
- tutorialHints는 Scene이 아닌 이유 설명 추가

#### 커밋
- `0bca0f5` — `docs: Phase 4 구현 반영 문서 업데이트 (06/13/14/README)`

---

### 세션 4: Exponential Decay Smoothing 적용 (4곳)

#### 배경
- "심플 피드백" (Steve Rabin, GDC AI Summit 2015) 기반
- 공식: `target + (current - target) * exp(-k * dt)` — 프레임 독립적
- k 파라미터: 높을수록 빠름, `1/k`초에 ~63% 도달

#### 공통 모듈
- `src/00_common/mathUtil.lua` (신규) — `expDecay(current, target, k, dt)` 함수

#### 적용 대상 (우선순위순)

| # | 대상 | k값 | 효과 |
|---|------|-----|------|
| 1 | 카메라 팔로우 | 12 | 플레이어 추적 시 부드러운 지연 |
| 2 | XP 오브 자석 | 8 | 끌려올 때 곡선 궤적 |
| 3 | 보스 HP바 | 4 | 데미지 시 흰색 고스트바 서서히 줄어듦 |
| 4 | 레벨업 카드 등장 | 6 | 아래에서 위로 순차 슬라이드+페이드인 |

#### 파일 변경 목록
**신규:**
- `src/00_common/mathUtil.lua` — exp decay 공통 유틸

**수정:**
- `src/02_renderer/cameraManager.lua` — `mathUtil.expDecay` 카메라 추적
- `src/03_game/systems/xpCollectionSystem.lua` — 자석 속도에 expDecay
- `src/04_ui/topHud.lua` — `update(dt)` 추가 + bossDisplayHp 듀얼 HP바
- `src/04_ui/uiManager.lua` — `topHud.update(dt)` 호출 추가
- `src/03_game/states/levelUp.lua` — 카드 entrance 애니메이션 (wall-clock 기반)

#### 설계 포인트
- levelUp은 `timeScale=0`이라 game dt 사용 불가 → `love.timer.getTime()` wall-clock 사용
- touch 판정은 최종 위치(cardY) 사용 → 애니메이션과 독립
- 상수 `CARD_ANIM_K`, `CARD_STAGGER`는 파일 상단 local로 배치 (매 프레임 재선언 방지)

#### 테스트 결과
- [x] 카메라 부드럽게 따라감
- [x] XP 오브 곡선 궤적
- [x] 보스 HP 고스트바 슬라이딩
- [x] 레벨업 카드 순차 슬라이드+페이드인
- [x] 크래시 없음
