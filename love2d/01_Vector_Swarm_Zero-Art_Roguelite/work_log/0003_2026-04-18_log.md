# 0003_2026-04-18_log.md

## 작업 요약: Phase 3A 보스 시스템 MVP 구현

---

### 완료된 작업

#### 1. 보스 설계 문서 작성 ✅

- `doc/08_BOSS_SYSTEM_DESIGN.md` 작성 (planner 워크플로우 전체 적용)
- 재미 검증 (4기둥 전부 기여)
- 설계 결정 5건 (구현 방식, 스테이지 배치, 페이즈 시스템, 등장 방식, 탄막 소거)
- ECS 분해 (컴포넌트 1종, 시스템 1종, 기존 수정 6건)
- 보스 5종 상세 설계 (NULL ~ OVERFLOW)
- 보스 클리어 보상 시퀀스 (6단계: 폭발 → 변환 → 회복 → 텍스트 → 수집 → 진입)
- 밸런스 프레임워크 (DPS 역산 기반 HP 산출)
- 작업 목록 12건 + 검증 기준 10건

#### 2. Phase 3A MVP 구현 ✅

**신규 파일:**
- `src/03_game/components/bossTag.lua` — BossTag 컴포넌트 (페이즈, 패턴 순환, 인트로/처치 플래그)
- `src/03_game/systems/bossSystem.lua` — BossSystem (인트로 → 페이즈 전환 → 패턴 순환 → 이동)

**수정 파일:**
- `src/03_game/entities/entityFactory.lua` — `createBoss()` + BOSS_TYPES (NULL, STACK)
- `src/03_game/systems/stageManager.lua` — 보스 스테이지 감지, 상태 머신 (BOSS_INTRO → BOSS_ACTIVE → BOSS_CLEAR)
- `src/03_game/systems/enemyCollisionSystem.lua` — BossTag 엔티티 처치 시 대량 XP 드롭 (25개 오브 폭발)
- `src/03_game/systems/bulletPool.lua` — `clearLayer()` 함수 추가
- `src/04_ui/topHud.lua` — 보스 HP바 + 보스명 + 페이즈 마커 표시
- `src/01_core/ecsManager.lua` — BossSystem 등록, renderSystems 최적화
- `src/main.lua` — 보스 HUD 데이터 연동, 보스 처치 보상 (HP/대쉬/포커스 회복, 히트스톱)

#### 3. 보스 프리셋 2종 ✅

| 보스 | 스테이지 | HP | 페이즈 | 패턴 | 색상 |
|------|:---:|:---:|:---:|------|------|
| NULL | 3 | 50 | 2 | circle → circle+spiral | 회색 |
| STACK | 6 | 100 | 3 | circle → circle+aimed → dense+aimed | 초록 |

#### 4. 디버그 기능 ✅

- **F7: God Mode** (플레이어 무적 토글)
- debug watch panel에 GOD 상태 표시
- 로거 콘솔에 ON/OFF 로그 출력

#### 5. 코드 리뷰 2회 (refactor-cleaner) ✅

**1차 리뷰 (전체 스캔):** 22건 발견 → 6건 수정
- bossSystem: 패턴 적용 3중 중복 → `applyPattern()` 헬퍼 추출
- bossSystem: `require("01_core.world")` 루프 내 호출 → 모듈 레벨 이동
- bossSystem: `ai` nil 가드 추가
- ecsManager: `renderSystems` 매 프레임 테이블 생성 → 모듈 상수
- bossTag: 미사용 `defeatTimer` 필드 제거
- topHud: 미사용 `profile` 변수 제거

**2차 리뷰 (수정 검증):** 5건 발견 → 2건 수정
- bossSystem: 미사용 변수 `px` → `_` 처리
- ecsManager: `bulletPool`/`stageManager` nil 가드 추가

#### 6. 버그 수정 3건 ✅

| 버그 | 원인 | 수정 |
|------|------|------|
| 줌아웃 시 크래시 | main.lua 313줄 `w` 미선언 상태 접근 | `local bw = ecsManager.getWorld()` 별도 변수 |
| 보스 처치 후 안 사라짐 | stageManager BOSS_CLEAR에서 엔티티 destroy 누락 | `w:destroyEntity(bossEntityId)` 추가 |
| STACK 보스 화면 밖 이탈 | BOSS_TYPES 미정의 + bossSystem 이동 로직 NULL만 처리 | STACK 프리셋 추가 + 보스 이동 리팩터 (default fallback) |

---

### 현재 키 바인딩

| 키 | 기능 |
|----|------|
| `` ` `` | Logger 콘솔 토글 |
| F1 | Debug watch panel 토글 |
| F2 | UI 표시 토글 |
| F3 | UI 디버그 모드 토글 |
| F4 | Screen grid 토글 |
| F5 | Camera 모드 전환 (game ↔ debug) |
| F7 | **God mode 토글 (NEW)** |
| ESC | 게임 종료 |

---

### 파일 변경 목록

**신규:**
- `doc/08_BOSS_SYSTEM_DESIGN.md`
- `src/03_game/components/bossTag.lua`
- `src/03_game/systems/bossSystem.lua`

**수정:**
- `src/main.lua` — 보스 HUD 연동, 보상 시퀀스, godMode, 버그 수정
- `src/01_core/ecsManager.lua` — BossSystem 등록, renderSystems 최적화, nil 가드
- `src/03_game/entities/entityFactory.lua` — createBoss, BOSS_TYPES (NULL, STACK)
- `src/03_game/systems/stageManager.lua` — 보스 상태 머신, 엔티티 destroy
- `src/03_game/systems/enemyCollisionSystem.lua` — 보스 처치 XP 드롭
- `src/03_game/systems/bulletPool.lua` — clearLayer()
- `src/04_ui/topHud.lua` — 보스 HP바, 미사용 변수 정리
- `doc/00_PROJECT_ROADMAP.md` — Phase 3A 진행 상황 반영

---

### 다음 작업 (예정)

**Phase 3A 잔여:**
- [x] HEAP 보스 프리셋 (Stage 9, 랜덤 burst + 텔레포트) ✅
- [x] RECURSION 보스 + 미니언 소환 (Stage 12) ✅
- [x] OVERFLOW 최종 보스 (Stage 15, 4Phase 폭주) ✅

**Phase 3B: 적 다양성**
- [ ] 세계관 기반 적 6종 (Bit, Node, Vector, Loop, Matrix, Worm)

**Phase 3C: 비주얼**
- [ ] 보스 처치 연출 풀 버전 (탄막→XP 변환, 회복 애니메이션, 글리치 텍스트)
- [ ] 네온 블룸/Glow 셰이더

---

### 주요 설계 결정 (Day 4)

| 결정 | 선택 | 이유 |
|------|------|------|
| 보스 구현 방식 | 기존 Enemy + BossTag 확장 | ECS 강점 활용, 코드 중복 최소 |
| 보스 스테이지 배치 | Stage 3, 6, 9, 12, 15 | ~1분40초에 첫 조우, 모바일 세션 최적 |
| 페이즈 시스템 | HP 기반 전환 + 시간 기반 패턴 순환 | 이중 긴장감 |
| 탄막 소거 | MVP: 전소거 → 3C: 연출 소거 | 단계적 품질 향상 |
| 보상 시퀀스 | 6단계 (폭발→변환→회복→텍스트→수집→진입) | 감정 곡선 설계 |
| HEAP 이동 | 텔레포트 + 점멸 경고 | 위치 예측 긴장감, 회피 판단 재미 |
| RECURSION 미니언 | 주기적 보충 + cap | 지속적 압박감, Phase 전환 리셋으로 임팩트 |
| OVERFLOW 컨셉 | "폭주" (전패턴 순환 + Phase별 이동 전환) | 복습시험 → 놀라움+압박으로 변경 |
| OVERFLOW 밸런싱 | P4 미니언 3마리 유지 (5 아닌) | 인지 과부하 방지, 속도로 압박 |
| handleMovement 리팩터 | bossType → teleportInterval 기반 | 데이터만으로 이동 모드 결정, 확장성 |
| 회귀 테스트 도입 | 리팩터링 후 기존 기능 검증 | end 누락 버그 경험에서 학습 |

---

### HEAP 보스 (Stage 9) 구현 상세

- **텔레포트**: 5~3초 간격, 1초 경고 점멸 → 즉시 이동 + 무적 0.3초
- **Phase별 간격**: P1=5s, P2=4s, P3=3s (점점 빈도 증가)
- **bossTag 필드 추가**: teleportInterval, teleportTimer, teleportWarning, teleportCooldown, teleporting
- **F8 스테이지 스킵** 디버그 기능 추가 (stageManager.debugSkipStage)
- **보상 중복 버그 수정**: bossRewardsApplied 플래그로 1회 보장

### RECURSION 보스 (Stage 12) 구현 상세

- **orbit 이동**: 플레이어 주변 반경 3.5, 속도 0.8로 공전 (EnemyAISystem 활용)
- **미니언 소환**: Phase별 config — P1: 없음, P2: max 3/8초, P3: max 5/6초
- **Phase 전환 시 미니언 전소거**: handlePhaseCheck에서 보스 외 EnemyAI 엔티티 일괄 파괴
- **handleMinionSpawn 헬퍼**: 주기적 보충, cap 초과 방지, 보스 주변 원형 배치
- **bossTag 필드 추가**: minion (per-phase config table), minionTimer
- **bossSystem end 누락 버그 수정**: handleMovement else 분기 end 짝 불일치

### OVERFLOW 보스 (Stage 15) 구현 상세

- **4 Phase 감정 곡선**: P1 "편하네" → P2 "좀 세지네" → P3 "텔포까지?!" → P4 "넘친다!"
- **Phase별 색상 변화**: 빨강(NULL) → 초록(STACK) → 보라(HEAP+RECURSION) → 흰색(전부)
- **Phase별 이동 전환**: P1~P2 drift → P3 텔레포트 5초 → P4 텔레포트 3초
- **Phase별 미니언**: P1~P2 없음, P3~P4 max 3마리/8초
- **핵심 리팩터**: handleMovement 분기를 `bossType` → `teleportInterval > 0` 데이터 기반으로 변경
- **bossTag 필드 추가**: phaseTeleport (Phase별 teleport interval), phaseColors (Phase별 색상)
- **handlePhaseCheck 확장**: Phase 전환 시 색상/텔레포트 간격 자동 업데이트
- **회귀 테스트**: 기존 보스 4종 (NULL/STACK/HEAP/RECURSION) 전부 정상 확인

### 밸런스 검토 (OVERFLOW 전 전체 점검)

- bullet_damage +1 업그레이드가 DPS(초당 피해량) 100% 증가로 과도함 → Phase 3A 완료 후 별도 밸런스 패스 예정
- 보스 TTK(처치 시간)가 기본 DPS 기준 너무 길지만, 레벨업 성장 고려 시 적절
- OVERFLOW Phase 4: 미니언 3마리(5마리 아닌) + 빠른 텔레포트로 "읽을 수 있는 폭주" 설계

### Phase 3A 보스 시스템 완료! 🎉

| 보스 | Stage | HP | Phase | 고유 메카닉 |
|------|:---:|:---:|:---:|------|
| NULL | 3 | 50 | 2 | — (튜토리얼) |
| STACK | 6 | 100 | 3 | circle + aimed 조합 |
| HEAP | 9 | 160 | 3 | 텔레포트 + 점멸 경고 |
| RECURSION | 12 | 200 | 3 | orbit 이동 + 미니언 소환 |
| OVERFLOW | 15 | 300 | 4 | 전 패턴 순환 + Phase별 이동/색상 전환 |

---

## Day 4 오후 세션 2 — 기술 부채 정리 + 개발 환경 개선

### 자동 챙김 규칙 추가 ✅
- `.github/copilot-instructions.md`에 3가지 규칙 추가:
  1. 커밋 시 work_log 같이 업데이트
  2. 기능 완료 시 로드맵 체크박스 업데이트
  3. 3회+ 커밋 후 push 안 했으면 알림

### 기술 부채 수정 ✅

| 이슈 | 파일 | 수정 내용 |
|------|------|----------|
| 컴포넌트 테이블 공유 참조 | collider, renderable, bulletEmitter, playerWeapon | defaults 테이블 fallback → inline literal로 매번 새 테이블 생성 |
| emitter.angle 오버플로 | bulletEmitterSystem.lua | spiral/wave 패턴 angle에 `% pi2` modulo 적용 (2곳) |
| player.draw() 호출 잔재 | main.lua | 삭제된 함수 호출 제거 (PlayerRenderSystem이 대체) |
| gameState 폰트 캐시 | — | 이미 lazy init 적용됨, 로드맵 메모만 outdated → 정리 |

### 개발 환경 개선 ✅

| 항목 | 수정 |
|------|------|
| 콘솔 출력 | love.exe → lovec.exe (tasks.json + run.bat) |
| conf.lua | `t.console = false` (lovec.exe는 자체 콘솔 앱이라 별도 창 불필요) |
| run.bat | `start ""` 제거 (프로세스 분리 방지, stdout 연결 유지) |

### 로드맵 갱신 ✅
- 3A.10~12 (HEAP/RECURSION/OVERFLOW) 체크박스 ✅ 반영
- 기술 부채 메모: 폰트 캐시, angle 오버플로, 공유 참조 → 완료 처리
- HEAP/RECURSION/OVERFLOW 프리셋 → 완료 처리

### 커밋
- `258e2ac` — docs: 자동 챙김 규칙 추가
- `31503a8` — fix: 공유 참조 버그 4건 + angle 오버플로 + player.draw 호출 제거 + lovec 콘솔 출력 설정

### 다음 작업 (합의된 순서)
1. 적 처치 파편 파티클 (2A.3) — 🎁 보상 타이밍
2. 밸런스 패스
3. mobileLayout GC + ecs._indexSize
4. 3C.1 Bloom 셰이더
5. 3B 적 다양성

## Day 4 종료 시점 현황

| 항목 | 수치 |
|------|------|
| 컴포넌트 | 17종 (+BossTag) |
| 시스템 | 15종 + StageManager (+BossSystem) |
| 적 타입 | 4종 (기존) |
| 보스 타입 | 2종 (NULL, STACK) / 5종 계획 |
| 문서 | 9개 (00~08) |
| 완료 Phase | 0, 1, 2A~2D, 3A MVP |

---

## Day 4 오후 (Windows PC 이어서 작업)

### 개발 환경 개선 ✅

- `.github/prompts/resume.prompt.md` — 작업 재개 프롬프트 (기기 간 공유)
- `.github/copilot-instructions.md` — 개발 워크플로우 6단계 규칙 추가 + 작업 로그 규칙
- 작업 흐름: 계획 → 구현 → 코드 리뷰 → 테스트 → 검증 → 커밋

### HEAP 보스 프리셋 (3A.10) ✅

**신규/수정 파일:**
- `src/03_game/components/bossTag.lua` — 텔레포트 필드 5개 추가 (teleportTimer, teleportInterval, teleportWarning, teleportCooldown, teleporting)
- `src/03_game/entities/entityFactory.lua` — BOSS_TYPES.HEAP 프리셋 추가, createBoss()에 텔레포트 파라미터 전달
- `src/03_game/systems/bossSystem.lua` — HEAP 전용 이동 로직 (랜덤 텔레포트 + 깜빡임 경고)

| 파라미터 | 값 |
|----------|-----|
| HP | 160 |
| 페이즈 | 3 (66%, 33%) |
| 히트박스 | 0.9 |
| 색상 | {0.9, 0.3, 0.1} (주황) |
| XP 보상 | 200 |
| 텔레포트 | 5초 간격, 1초 전 경고 깜빡임, 0.3초 후 무적 |

**패턴:**
- Phase 1: circle(10발) → 쉼 → circle(6발 빠르게) → 쉼
- Phase 2: circle(12발) → wave(불규칙) → 쉼
- Phase 3: dense circle(18발) → aimed(4발 연사) → 짧은 쉼

**설계 결정:**
| 결정 | 선택 | 이유 |
|------|------|------|
| 텔레포트 방식 | 순간이동 + 깜빡임 경고 | "읽을 수 있는 위험"으로 재미 유지, 기존 렌더링 시스템 활용 |

### 디버그 기능 추가 ✅

- **F8: 스테이지 스킵** — 모든 적/보스 즉시 제거 + 다음 스테이지로
- `src/03_game/systems/stageManager.lua` — `debugSkipStage()` 메서드 추가
- `src/01_core/ecsManager.lua` — `getStageManager()` 접근자 추가
- `src/main.lua` — F8 키 바인딩

### 버그 수정 ✅

| 버그 | 원인 | 수정 |
|------|------|------|
| 보스 처치 보상 4회 중복 적용 | `clearTimer < 0.05` 조건이 여러 프레임 통과 | `bossRewardsApplied` boolean 플래그로 1회 보장 |
