# 0007_2026-04-22_log.md

## 작업 요약: 5A.3f STAGE_DEFS 확장 (변형/포메이션 배치 규칙)

---

### 세션 1: 5A.3f 계획 수립

#### 설계 결정

**목표**: 변형 4종 + 포메이션 5종이 실제 스테이지에서 의미 있게 등장하도록 규칙 시스템 구축

**접근법 비교**:
| 선택지 | 내용 | 판정 |
|--------|------|------|
| A. 전부 손작업 | Stage 1~15 개별 데이터 정의 | ❌ 데이터 물량 = 제약의 미학 위반 |
| B. 규칙만 | 글로벌 규칙 강화 | ❌ "특별한 순간" 보장 안 됨 |
| **C. 하이브리드 (규칙 중심)** | 핵심 순간 보장 + 나머지 규칙 | ✅ 3줄 규칙 = 15개 테이블 대체 |

**가치관 검증**:
- **재미**: ✅ 4기둥 모두 기여 (긴장감+놀라움+성취감+몰입감)
- **쾌감**: ✅ 변형 "첫 만남" 보장 → 놀라움 극대화
- **제약의 미학**: ✅ 코드 규칙으로 무한 스테이지 다양성 자동 생성

#### 4가지 규칙 설계

1. **스테이지 테마**: 기동형(bit/vector)/화력형(node/loop/matrix) 교대 → 스테이지마다 다른 느낌
2. **변형 보장 등장**: Stage 4/7/10/13 Wave 1에 해당 변형 1체 확정 스폰
3. **변형 확률 스케일링**: baseChance × (1 + (stage-first) × 0.03), cap 25%/전체 50%
4. **포메이션 + 변형 조합**: 포메이션 내 적에게도 변형 적용

#### 문서 업데이트
- `doc/07_STAGE_DESIGN_GUIDE.md` — §2.1 규칙 4종 상세 설계 추가, §5 확장 계획 현행화, 적 타입명 현행화
- `work_log/0007` — 오늘 로그 생성

#### 수정 대상 (구현 예정)
- `src/03_game/systems/stageManager.lua` — 1개 파일만 수정
  - `_getStageConfig()`: 테마 규칙 (적 풀 결정)
  - `_spawnWave()`: 변형 보장 등장
  - `_pickVariant()`: 확률 스케일링
  - `_spawnFormation()`: 변형 파라미터 전달

---

### 세션 2: 구현 + 테스트 + 버그 수정

#### 구현
- `stageManager.lua` 단일 파일 수정 (4개 함수/섹션)
  - 테마 풀(`MOBILITY_POOL`/`FIREPOWER_POOL`) + `_getStageConfig()` 테마 교대
  - `GUARANTEED_VARIANTS` + `_spawnWave()` wave 1 보장 등장
  - `VARIANT_TIERS` + `_pickVariant()` 스케일링 확률
  - `_spawnFormation()` guaranteedVariant 5번째 파라미터 추가

#### 테스트 결과 (F8 스테이지 스킵)
| Stage | 기대 | 실제 | 결과 |
|-------|------|------|------|
| 1~5 | [HAND] | [HAND] | ✅ |
| 4 | Guaranteed swift | swift-bit 확인 | ✅ |
| 7 | [MOB] + Guaranteed armored | armored-vector/armored-loop 확인 | ✅ |
| 8 | [FP] | node/loop/matrix 확인 | ✅ |
| 10 | [MOB] + Guaranteed splitter | splitter-bit 확인 | ✅ |
| 13 | [MOB] + Guaranteed shielded | shielded-vector 확인 | ✅ |
| 14 | [FP] | loop/matrix 혼합 확인 | ✅ |
| 16 | [MOB] | bit/vector/loop 혼합 확인 | ✅ |
| 17 | [FP] | node/loop/matrix 혼합 확인 | ✅ |
| 18 | 보스 이후 정상 진행 | ❌→✅ (버그 수정 후 통과) | ✅ |

#### 버그 수정
- **Stage 18 크래시**: `_pickVariant` 함수가 `_spawnFormation` 뒤에 정의됨 → Lua local function 순서 문제
- **해결**: `_pickVariant` + 관련 상수(`GUARANTEED_VARIANTS`, `VARIANT_TIERS`, `VARIANT_CAP`)를 `_spawnFormation` 위로 이동

#### main.lua 에러 핸들러 추가 (부수 작업)
- `love.errorhandler()` 커스텀 핸들러: 크래시 시 `debug.log`에 기록 후 기본 에러 화면 표시
- `debug` 모듈 이름 충돌 해결: `local debug` → `local debugOverlay` (Lua 표준 `debug.traceback` 보호)

---

### 완료
- [x] stageManager.lua 구현 (4개 함수 수정)
- [x] F8 스테이지 스킵으로 전 스테이지 검증
- [x] doc/00 로드맵 5A.3f ✅ + 커밋

---

### 세션 3: 5A.4 보스 비주얼 연출 강화 — 계획

#### 현황 분석

| 장면 | 현재 | 문제 |
|------|------|------|
| 인트로 | 뿅 등장 → 1.5s 대기 → 작은 흔들림 | 긴장감 제로 |
| 페이즈 전환 | screenShake + 탄막 전소거 | "바뀌었다" 인지 약함 |
| 처치 | 히트스톱 0.4s + 흔들림 + 탄막→XP | 카타르시스 부족 |

#### 설계 결정

**선택지 비교**:
| 선택지 | 내용 | 판정 |
|--------|------|------|
| A. 전부 구현 (인트로+페이즈+처치) | 보스 전체 경험 완성 | ✅ 채택 |
| B. 인트로+페이즈만 | 처치는 기존으로 | △ 처치가 가장 중요한 순간인데 빈약 |
| C. 인트로만 | 임팩트 큰 것 하나만 | ✗ 보스전 체감 반쪽 |

**가치관 검증**:
- **재미**: ✅ 보스전 전체 흐름에 긴장감→위기감→카타르시스 곡선 부여
- **쾌감**: ✅ 플래시/히트스톱/파동 — 격투게임급 피드백
- **제약의 미학**: ✅ 도형+색상+타이머만으로 연출 (Zero-Art)

#### 3개 연출 상세 → `doc/08_BOSS_SYSTEM_DESIGN.md` §5.6에 기록

#### 수정 대상
- `bossSystem.lua` — 인트로 스케일, 페이즈 히트스톱/스케일 펄스
- `playScene.lua` — 화면 플래시/틴트/화이트아웃
- `renderSystem.lua` — 보스 스케일 반영
- `stageManager.lua` — 인트로 글리치 텍스트
- `entityFactory.lua` — 보스 초기 scale=0

#### 구현 결과
- `bossSystem.lua`: handleIntro에 ease-out cubic 스케일인 + introFlash 플래그, handlePhaseCheck에 scalePulse/phaseHitStop/phaseFlash 플래그, 메인 루프에 scalePulse exp 감쇠
- `playScene.lua`: 5종 이펙트 상태 (_screenFlash, _screenTint, _whiteout, _shockwave, _phaseText) + update 감쇠 + draw 렌더 + 보스 플래그 감지
- `renderSystem.lua`: 보스 5종 렌더에 `r = r * (transform.scale or 1)` 추가
- `stageManager.lua`: 인트로 텍스트에 bossTag.introTimer 기반 글리치 효과 ("< NULL >")
- `entityFactory.lua`: createBoss에 `scale = 0` (인트로 스케일인 시작)

#### refactor-cleaner 점검
- `math.exp` → `_exp` 캐시 추가 (bossSystem.lua)
- dead code / 중복 / 레이어 위반 없음 확인

#### 테스트 결과 (F8 + 실전 플레이)
| 보스 | 인트로 | 페이즈 전환 | 처치 | 결과 |
|------|--------|-----------|------|------|
| NULL (Stage 3) | 스케일인 + 글리치텍스트 + 플래시 | phase 1→2 히트스톱+틴트+펄스 | 화이트아웃+쇼크웨이브 | ✅ |
| STACK (Stage 6) | 스케일인 + 글리치텍스트 + 플래시 | phase 1→2→3 | 화이트아웃+쇼크웨이브 | ✅ |
| HEAP (Stage 9) | 스케일인 + 글리치텍스트 + 플래시 | phase 1→2→3 | 화이트아웃+쇼크웨이브 | ✅ |
| 크래시 | — | — | — | ERROR 없음 ✅ |

---

### 완료
- [x] 5A.3f STAGE_DEFS 확장 (테마 규칙 4종)
- [x] 5A.4 보스 비주얼 연출 강화 (인트로+페이즈+처치)

---

### 세션 4: 5B 엔드게임 — Victory + Endless + Achievement

#### 설계 결정

**D안 채택 (하이브리드)**: 5종 보스 순환 × 라운드 스케일링
- A(단순 순환): 가치관 위반 — 반복 = 재미 소멸
- C(DNA 자동생성): doc/15 Phase A~E 전체 필요 → 과함
- **D**: 순환 + HP×1.5^round + 탄막 밀도 증가 + 색조 시프트 → 지금 실현 가능 + 나중에 C로 확장

#### 수정 대상 (7개 파일)
1. `gameState.lua` — VICTORY 상태 추가
2. `saveData.lua` — gameClear, bestEndlessStage 필드
3. `victoryScene.lua` — **NEW** Victory 오버레이
4. `stageManager.lua` — Endless 루프 + 보스 스케일링
5. `entityFactory.lua` — 보스 스케일링 파라미터
6. `playScene.lua` — Victory 연동
7. `topHud.lua` — Endless 표기
8. `achievementSystem.lua` — SYSTEM.EXIT(0) 도전과제
9. `bossTag.lua` — speedMult, minionAdd 필드
10. `bossSystem.lua` — 탄막 속도 × speedMult, 미니언 수 + minionAdd
11. `background.lua` — Endless 다크레드 테마

#### 코드 리뷰 수정 (clean-code-guide)
- victoryScene: `lg.newFont()` 매 프레임 생성 → lazy-cached 로 변경
- victoryScene: `math.min/max` → `_min/_max` 캐시
- victoryScene: `saveData.recordVictory()` 이중 호출 제거 (onVictory에서만)
- stageManager: `bossScaling` → new()에 명시 + boss_clear에서 nil 초기화

#### 테스트 결과
| # | 항목 | 결과 |
|---|------|------|
| 1 | 게임 시작 | ✅ |
| 2 | Stage 15 OVERFLOW 처치 → Victory | ✅ |
| 3 | 글리치 텍스트 + 로어 + 통계 | ✅ |
| 4 | ESC → 타이틀 + 런 저장 | ✅ |
| 5 | C → Endless Stage 16 | ✅ |
| 6 | 배경 다크레드 전환 | ✅ |
| 7 | Stage 16 적 스폰 정상 | ✅ |
| 8 | SYSTEM.EXIT(0) 도전과제 해금 | ✅ |
| 9 | HUD "ENDLESS" 표기 | ✅ |

#### 버그 수정: F8 Victory 전파
- **증상**: VictoryScene 중 F8이 PlayScene debugSkipStage로 전파
- **원인**: VictoryScene에서 키를 `return false`로 무시 → SceneStack이 아래 Scene으로 전달
- **수정** (3중 방어):
  1. `victoryScene.lua` — 모든 키 `return true` (4.5초 전/후 모두)
  2. `playScene.lua` — F8에 `isVictory()` 가드
  3. `stageManager.lua` — `debugSkipStage`에 `isVictory()` early return
- `victoryTriggered` 필드 → StageManager.new()에 명시적 초기화

---

### 완료
- [x] 5B.1 Victory Scene (글리치 텍스트 + 통계 + C/ESC)
- [x] 5B.2 Endless 모드 (5보스 순환 × 라운드 스케일링)
- [x] 5B.3 첫 클리어 도전과제 (SYSTEM.EXIT(0))
- [x] 5B.4 Endless 배경 다크레드
- [x] F8 Victory 전파 버그 수정

---

### 세션 5: 리팩토링 반영 문서 일괄 업데이트

이전 세션들(bossDefs 분리, renderSystem Strategy Pattern, stageData+formationDefs 분리)의 
코드 변경사항이 설계 문서에 반영 안 된 상태 → 5개 문서 한번에 업데이트.

#### 수정 내역

1. **04_TECHNICAL_ARCHITECTURE.md** (대규모)
   - 폴더 트리 전면 재작성 (존재하지 않는 kutil.lua/math/ 제거, 실제 파일 전체 반영)
   - ecsManager 경로 01_core → 03_game
   - data/, renderers/, scenes/, states/ 전체 나열
   - 05_sound/ 레이어 추가, minimap.lua 추가
   - 적 프리셋 basic/spiral/aimed/wave → bit/node/vector/loop/matrix
   - gameState API 업데이트 (isVictory, addFragments, triggerVictory)
   - states 7종 테이블 추가
   - Scene Stack 섹션 신규 (§9.5)
   - 수학 라이브러리 → mathUtil.lua 교체 (§13)
   - 데이터 주도 아키텍처 섹션 신규 (§15)
   - Strategy Pattern 렌더 섹션 신규 (§16)

2. **08_BOSS_SYSTEM_DESIGN.md** — entityFactory → bossDefs.lua 6곳 수정
3. **09_ENEMY_DIVERSITY_DESIGN.md** — renderSystem → basicShapes/variantOverlays, stageManager → stageData/formationDefs 10곳 수정
4. **07_STAGE_DESIGN_GUIDE.md** — stageData/formationDefs 데이터 분리 안내 2곳 추가
5. **00_PROJECT_ROADMAP.md** — 기술부채 "적 비주얼 획일화" 완료 체크

---

### 세션 6: 4D.1 밸런싱 & QA

#### 문제 분석

**테스트로 발견한 3대 문제**:
1. **DPS 폭증** — Multi Shot(+2, 감쇠 없음) × Damage(+1, 감쇠 없음) = 제곱 스케일. Stage 6 STACK 0.4초 전투
2. **XP 오브 색상** — XP 오브 {0.2,1.0,0.4} ≈ 플레이어 탄환 {0.2,1.0,0.5} — 구분 불가
3. **보스 패턴 미관람** — 보스가 너무 빨리 죽어서 페이즈 전환 연출/탄막 패턴을 볼 수 없음

#### 8개 밸런스 결정 (이전 세션에서 합의)
1. Damage 감쇠 면제 ✅
2. Variant XP shielded↔armored 교환 ✅
3. Endless bulletSpeedMult + emitRateMult ✅
4. RECURSION HP 200→230 ✅
5. Data Miner 비용 130→75 (10/25/40) ✅
6. Focus recharge +0.15 비례 ✅
7. Multi Shot +2(홀수) + spread cap 0.35 ✅
8. 클리어 속도 (waves cap 10, enemy cap 20, spawnDelay 2.0, xpGrowth 1.3) ✅

#### DPS 억제 추가 수정 (테스트 후 발견)
- **Multi Shot maxPicks=2** (1→3→5발 cap, 이후 풀에서 제외)
- **xpGrowth 1.3→1.35** (레벨업 속도 절충)
- **XP 오브 금색** `{1.0, 0.85, 0.2}` (탄환과 완전 분리)

#### 보스 전투 개선 계획 (구현 예정)

| 항목 | 내용 | 파일 |
|------|------|------|
| **A. 페이즈 전환 무적 2초** | 전환 시 2초간 피격 불가 → 패턴 시연 보장 | bossSystem.lua |
| **B. 보스 HP 레벨 스케일링** | `baseHP × (1 + 0.15 × playerLv)`, 이진검색 튜닝 | entityFactory.lua, playScene.lua |
| **C. 스테이지 클리어 스토리텔링** | 수학/CS 연속 서사 — "Memory Odyssey" | stageStory.lua (신규), stageManager.lua |

#### C. 스토리텔링 설계 — "Memory Odyssey"

**컨셉**: 손상된 메모리 시스템을 순회하는 프로그램의 여정. 각 스테이지가 수학/CS 개념 하나.

**일반 스테이지** (글리치 텍스트, 2~3초 표시):

| Stage | 개념 | 텍스트 |
|-------|------|--------|
| 1 | Binary | `"01100010 — The world speaks in two."` |
| 2 | Logic Gate | `"AND, OR, NOT — truth has only three gates."` |
| 4 | Variable | `"x := chaos — a name given to the unnamed."` |
| 5 | Loop | `"while(alive) { fight(); } — same path, never the same."` |
| 7 | Array | `"arr[0]: You are here. Position is identity."` |
| 8 | Sort | `"O(n log n) — the price of order in entropy."` |
| 10 | Tree | `"if(left \|\| right) — every decision branches."` |
| 11 | Graph | `"V={you}, E={connections} — isolation is a node with no edges."` |
| 13 | Infinity | `"lim(n→∞) — not a place, but a direction."` |
| 14 | Chaos | `"δx₀=0.001 — deterministic, yet unpredictable."` |

**보스 스테이지** (팝업 씬, 별도 오버레이):

| Stage | 보스 | 제목 + 본문 |
|-------|------|-------------|
| 3 | NULL | **DEREFERENCED** — `"*ptr = NULL; — You followed a pointer to nothing..."` |
| 6 | STACK | **UNWOUND** — `"pop(); pop(); pop(); — Last in, first out..."` |
| 9 | HEAP | **FREED** — `"free(max); — Priority determined survival..."` |
| 12 | RECURSION | **BASE CASE** — `"return 1; — To understand recursion, first understand recursion..."` |
| 15 | OVERFLOW | **HANDLED** — `"catch(overflow) {} — When the count exceeds its container..."` |

Endless (16+): 짧은 수학 fact 순환

#### 수정 파일 목록
- `levelUp.lua` — Multi Shot maxPicks=2 ✅
- `playerXP.lua` — xpGrowth 1.35 ✅
- `entityFactory.lua` — XP 오브 금색 ✅ + 보스 HP 스케일링 (예정)
- `bossSystem.lua` — 페이즈 전환 무적 ✅
- `enemyCollisionSystem.lua` — 보스 무적 시 데미지 스킵 ✅
- `stageStory.lua` — 스토리 데이터 (신규) ✅
- `stageManager.lua` — 스토리 표시 + 보스 HP 스케일링 ✅
- `playScene.lua` — bossName nil guard ✅
- `doc/00_PROJECT_ROADMAP.md` — 4D.1 세부 항목 ✅

#### 추가 수정 (테스트 중 발견)
- **scaling.round nil 크래시 5곳 수정**: entityFactory, stageManager(3곳), playScene — `getBossScaling` or {} 변경 후 round가 nil인 비-Endless 보스에서 format 에러
- **NULL 보스 HP 50→150**: 실전투 0.9초 → ~15초로 개선
- **웨이브 딜레이 축소**: `max(2.0, 3.0-(s-1)*0.3)` → `max(1.2, 2.0-(s-1)*0.2)` — Stage 1: 3s→2s, 후반: 2s→1.2s
- **dead import 제거**: playerWeaponSystem.lua `_floor` 미사용 캐시 삭제

#### 테스트 결과
| # | 항목 | 결과 |
|---|------|------|
| 1 | Stage 1~2 클리어 + 글리치 스토리 텍스트 | ✅ |
| 2 | Stage 3 NULL 보스 스폰 (HP:87 = 150×1.75/Lv5) | ✅ |
| 3 | 보스 페이즈 무적 깜빡임 | ✅ |
| 4 | 보스 클리어 스토리 팝업 (DEREFERENCED) | ✅ |
| 5 | Stage 4~5 정상 진행 + 포메이션 | ✅ |
| 6 | Stage 6 STACK 보스 전투 (~7초, HP:265) | ✅ (개선 여지) |
| 7 | Stage 7 사망 (HP×1.90 + armored/swift) | ✅ 난이도 적절 |
| 8 | 웨이브 딜레이 체감 개선 | ✅ |
| 9 | 크래시 없음 | ✅ |

#### 밸런스 관찰
- STACK 보스 실전투 ~2초 → 다음 밸런스 패스에서 HP 상향 필요
- 웨이브 딜레이 축소로 전투 템포 개선
- xpGrowth 1.35 + Multi Shot cap 2회 → DPS 안정화

---

### 완료
- [x] 4D.1a DPS 스케일링 억제 (Multi Shot cap, xpGrowth)
- [x] 4D.1b XP 오브 금색
- [x] 4D.1c 보스 페이즈 전환 무적 2초
- [x] 4D.1d 보스 HP 레벨 스케일링
- [x] 4D.1e 스테이지 클리어 스토리텔링
- [x] 4D.1f 기타 밸런스
- [x] scaling.round nil 크래시 5곳 수정
- [x] NULL HP 50→150, 웨이브 딜레이 축소
- [x] clean-code-guide 리뷰 (dead import 1건 제거)

---

### 세션 7: 보스 HP 2차 튜닝 + CS 기반 적 DNA 시스템 설계

#### 보스 HP 2차 튜닝 (`159cbb7`)
- godMode(HP999+weapon ON)로 Stage 1~21 플레이테스트 후 데이터 분석
- NULL 150→200, STACK 250→350 (초반 보스 전투 시간 확보)
- Endless hpMult: `1.5^round` → `2.0^round` (Endless 보스 너무 빨리 녹는 문제)
- 결과: NULL 4.2s→9.4s, STACK 4.9s→18.4s (만족)

#### CS 기반 적 DNA 시스템 설계 (doc/15 대규모 재설계)
- DNA 랜덤 조합(81K종) → CS 진화론 + 변이 하이브리드로 전환
- 핵심: CS 개념이 적의 정체성(이름→형태→행동 일관성) 보장
- 2계층: CS 프리셋(수작업 15종) + 변이 엔진(자동 5,400+종)
- 1차 구현 대상: Linked List(체인 절단), Tree(프랙탈 분열), Hash(순간이동)
- Hash 밸런스: 경고→이동→경직 사이클 + 착지마커 + 최소거리

#### 용어집 신설 (doc/99_GLOSSARY.md)
- 30개+ 축약어/전문용어 정리
- 규칙: 첫 등장 시 `축약어 (Full Name)` 병기

---

### 세션 8: doc/15 §5.5 수학 곡선 시스템 상세화

#### 변경 내용 (doc/15_PROCEDURAL_ENEMY_DNA_DESIGN.md, +278/-20)

§5.5를 10행 테이블에서 **사전 계산 수학 곡선 시스템** 전체 레퍼런스로 확장:

1. **사전 계산 아키텍처** — `shapeDefs.lua` 구조 + renderSystem 사용법 코드 예시
   - 핵심: 런타임 sin/cos 제로. 정점 배열을 `lg.polygon("line", verts)` 1회로 렌더
2. **기존 5종 정리** — circle, diamond, arrow, spiral_ring, hexagon (basicShapes.lua)
3. **신규 Tier 1 곡선 10종** 각각에:
   - ASCII 시각화 (형태 직관)
   - 정확한 수식 + 정점 수 + Lua 생성 코드
   - CS 적 매핑 근거 (왜 이 곡선이 이 CS 개념인가)
   - Reuleaux→Encrypt, Astroid→Compress, Deltoid→Tree, Cardioid→Search, Rose→Neural, Superellipse→Stack, Lemniscate→Loop, Vesica→Search, Koch→Tree가지, Epicycloid→Encrypt
4. **Tier 2 테이블** — 탄막/이동 궤적 전용 4종 (lissajous, fibonacci_spiral, clifford_attractor, l_system)
5. **CS 적 ↔ 곡선 최종 매핑** — 10행 종합 테이블
6. **Phase 6A.0에 shapeDefs.lua 생성기 작업 추가**

---

### 세션 9: Phase 6B DNA 변이 엔진 설계 확정

#### 핵심 설계 결정

1. **구현 순서 역전**: 6A(CS 특수 적) 먼저 → **6B(DNA 변이 엔진) 먼저**로 변경
   - 이유: DNA 조합으로 외형 다양성을 먼저 확보, CS 특수 적은 나중에 교체/보완
   
2. **적용 범위**: B안 채택 — Stage 16+ Endless에서만 DNA 적용
   - Stage 1~15 기존 코드 일절 안 건드림 (안전)
   
3. **Body 유전자 = 레이어 배열** (핵심 아이디어)
   - 기존 도형을 `{shape, mode, scale, rot}` 레이어로 조합
   - 예: `[diamond-fill-0.6-45°] + [diamond-line-1.0-0°]` → 이중 다이아몬드
   - 수학 곡선이나 수작업 외형은 나중에 레이어 교체로 보완
   
4. **도형 풀 12종**: 기존 6종 + 신규 6종 (triangle, star, cross, tear, bowtie, gear)

5. **기존 Variant 시스템과의 관계**
   - Variant = DNA의 modifier 유전자와 거의 동일
   - 기존 인프라(VARIANT_DEFS, pickVariant, variantOverlays)를 일반화하면 DNA 엔진

#### doc/15 업데이트 내용
- §3.2 DNA 예시: body를 문자열 → 레이어 배열로 변경
- §3.3 신규: Body 레이어 조합 시스템 상세 (도형 풀, 파라미터, 조합 규칙, 예시, 렌더링)
- §3.4 스케일링 수치: 도형 12종 × 파라미터 × 레이어 = 수만+ 조합
- §5.5 제목 변경: 레이어 조합 + 수학 곡선 확장 (수학 곡선은 2차 확장용 레퍼런스로 유지)
- §7 구현 순서: 6B → 6A → 6C → 6D (6B 우선)
- §7 Phase 6B 테이블 재작성 (6B.0~6B.4, 파일별 명시)
- §8 미결정 사항 재정리 (6B 관련 우선)
- 헤더 상태 업데이트

#### 구현 계획 (3.5세션 예상)
- 6B.0: dnaDefs.lua 구조 + 유전자 풀 (0.5세션)
- 6B.1: Body 레이어 렌더러 + 신규 도형 6종 (1세션)
- 6B.2: 변이 엔진 (1세션)
- 6B.3: 스폰 연동 (0.5세션)
- 6B.4: 테스트 + 튜닝 (0.5세션)
