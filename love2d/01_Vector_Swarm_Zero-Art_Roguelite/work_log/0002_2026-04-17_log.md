# 0002_2026-04-17_log.md

## 작업 요약: ECS 구현 + 아키텍처 정비 + 코드 품질 개선

---

### 완료된 작업

#### 1. ECS 코어 시스템 구현 ✅

**구현된 파일:**
- `01_core/ecs.lua` — ECS 핵심 (엔티티/컴포넌트 관리, componentIndex 캐시, pivot query 최적화)
- `01_core/system.lua` — System 베이스 클래스 (성능 모니터링 내장)
- `01_core/ecsManager.lua` — 오케스트레이터 (시스템 등록, update/draw 분리, renderSystems 테이블)

**핵심 기능:** 엔티티 생성/제거(ID 재활용), 컴포넌트 CRUD, 시스템 순차 실행, 성능 통계

#### 2. 아키텍처 리뷰 & 자동화 도구 ✅

- 9개 아키텍처 이슈 발견 → 전부 수정 (pairs 순회 버그, 이중 카메라 변환, 렌더 시스템 이중 실행 등)
- `.agents/architect.md` — 아키텍처 분석 에이전트 생성
- `.skills/architecture-rules.md` — 9개 아키텍처 규칙 정리
- `.skills/clean-code-guide.md` — SOLID + Clean Code + Lua 관용구 + 게임 패턴 가이드

#### 3. Phase 2A: ECS 구조 분리 ✅

**컴포넌트 파일 분리 (03_game/components/):**
- transform, velocity, collider, renderable, lifespan, playerTag, input, worldBound
- 각각 `.name`, `.defaults`, `.new(data)` 패턴

**시스템 파일 분리 (03_game/systems/):**
- movementSystem, lifespanSystem, renderSystem, inputSystem, boundarySystem, playerRenderSystem

**엔티티 팩토리 (03_game/entities/entityFactory.lua):**
- `createPlayer()`, `createEnemy()` — 컴포넌트 `.new()` 생성자 사용

#### 4. Phase 2B: Player ECS 전환 ✅

- OOP player → ECS 파사드 패턴으로 전환
- `player.bind(ecs, id)` — ECS 엔티티에 파사드 바인딩
- `InputSystem` — 터치/마우스 입력 → velocity 컴포넌트 반영
- `BoundarySystem` — 월드 경계 제한
- `PlayerRenderSystem` — 삼각형 + 방향 표시 렌더링 (lineWidth 설정)
- `player.update()` — 존 감지/파워업 등 월드 인터랙션만 담당
- `player.draw()` — 비어있음 (PlayerRenderSystem이 처리)

#### 5. 디버그/카메라 도구 개선 ✅

| 이슈 | 변경 내용 | 키 바인딩 |
|------|----------|----------|
| Logger 콘솔 | 내부 toggle, debug.lua와 분리 | `` ` `` |
| Debug watch | font 파라미터 추가 (fonts.small) | F1 |
| GridDebugDraw | grid_debug_draw → gridDebugDraw 리네임 + toggle | F4 |
| CameraManager | gameCamera/debugCamera 분리, 마우스 드래그/휠 | F5 |

#### 6. Logger 정리 ✅

- `logger.error()` 중복 코드 제거 → `writeLog()` 내부에서 ERROR 시 stderr 추가
- `local err` 전역 누출 수정
- 이모지 로그 → ASCII 태그로 변환 (`[CAM]`, `[ECS]`, `[ENTITY]`, `[PLAYER]`, `[ZONE]`, `[WORLD]`)
- `init()` 초기 메시지 5개 → `logger.info()` 1줄로 간결화
- `drawConsole()` 키 안내 → 현재 바인딩 반영
- 콘솔 기본값 hidden (main.lua에서 toggleConsole 호출 제거)
- 260줄 → ~130줄 (50% 감소)

---

### 현재 키 바인딩

| 키 | 기능 |
|----|------|
| `` ` `` | Logger 콘솔 토글 |
| F1 | Debug watch panel 토글 |
| F2 | UI 표시 토글 |
| F3 | UI 디버그 모드 토글 |
| F4 | Screen grid 토글 |
| F5 | Camera 모드 전환 (game ↔ debug) |
| ESC | 게임 종료 |

---

### 파일 변경 목록

**신규:**
- `.agents/architect.md`
- `.skills/architecture-rules.md`, `.skills/clean-code-guide.md`
- `src/01_core/ecs.lua`, `src/01_core/system.lua`
- `src/02_renderer/cameraManager.lua`
- `src/03_game/components/` (8개 파일)
- `src/03_game/systems/` (6개 파일)
- `src/03_game/entities/entityFactory.lua`
- `src/00_common/gridDebugDraw.lua` (리네임)

**수정:**
- `src/main.lua` — ECS 통합, cameraManager, 키 바인딩, logger 호출 정리
- `src/01_core/ecsManager.lua` — 외부 시스템/팩토리 require, renderSystems 분리
- `src/00_common/logger.lua` — 전면 리팩토링
- `src/00_common/debug.lua` — font 파라미터, toggleConsole
- `src/01_core/world.lua` — 이모지 로그 제거
- `src/03_game/entities/player.lua` — ECS 파사드 전환

**삭제:**
- `src/00_common/grid_debug_draw.lua` (→ gridDebugDraw.lua로 리네임)

---

### 다음 작업 (예정)
- Phase 3A: 보스 시스템
- Phase 3B: 세계관 기반 적 6종
- Phase 3C: 네온 발광 셰이더 (Bloom/Glow)
- 적 처치 파편 파티클 효과 (2A.3)
- graze 시스템 (아슬아슬 회피 보상)

---

## 후반 작업 (Phase 1 ~ 2D 전체 구현)

#### 7. Phase 1: MVP 게임 루프 (`60b1937` ~ `83ce770`)
- **1.1-1.2** BulletPool (2000개 사전할당, zero-GC) + BulletEmitter 시스템
- 코드 리뷰 수정: duplicate require, active field, draw color reset (`b2c9d5c`)
- **1.4-1.5** 충돌 시스템 + HP + iFrame 깜빡임 (`00955ed`)
- **1.6-1.8** EnemyAI (drift/orbit/chase) + 적 4종 프리셋 + 웨이브 스포너 (`d9286e6`)
- **1.9-1.10** GameState + 게임오버 오버레이 + R키/터치 리스타트 (`83ce770`)
- 로드맵 문서 생성 + gameState 폰트 캐시 수정 (`8e45b4e`)
- Reset 버튼 콜백 연동 (`89edcbd`)

#### 8. Phase 2A: 플레이어 자동공격 (`aad435b`)
- 가까운 적 자동 조준·발사 (WeaponSystem + PlayerAutoAttack)
- 플레이어 불릿 → 적 충돌, HP 차감, 처치
- 적 처치 시 XP 오브 드롭 기반 마련

#### 9. Phase 2B: 대쉬 & 포커스 (`94fc670`)
- 스와이프 대쉬 (순간이동 + 무적 + 쿨타임 3초)
- 대쉬 쿨타임 HUD 표시
- 포커스 모드 (Space → 슬로모 + 정밀이동 + 에너지 게이지)

#### 10. 코드 리뷰 & 리팩터 (`d7460e0`)
- refactor-cleaner 에이전트 활용, 미사용 변수 제거, 콜백 연결 정리

#### 11. Phase 2C: XP & 레벨업 (`f78b515`)
- 적 처치 → XP 획득 (자석식 수집)
- 레벨업 3택 1 선택 UI
- 무기 업그레이드 옵션 (발사속도/탄수/관통 등) + 패시브 스킬 옵션

#### 12. 문서 정리 (`59e0baa`, `66e3d60`, `c796e12`)
- 로드맵 파일명 변경 (00 prefix)
- 플레이 가이드 추가 (PC + 모바일)
- 문서 통합·삭제·넘버링 (12개 → 7개)

#### 13. 월드 리사이즈 (`97bb50b`)
- 120×250 → 20×30 아레나로 축소
- bulletBounds 비대칭 버그 수정 (world.getBounds() 사용)
- player.lua 월드 바운드 fallback 추가

#### 14. Planner 에이전트 보강 (`da09926`, `c7bb33b`)
- `.agents/planner.md` 7단계 워크플로우 재설계 (현황파악+코드스캔 → 재미검증 → 설계결정 → ECS분해 → 작업계획 → 밸런스검토 → 합의)
- `.skills/planning-guide.md` 신규 (재미 원칙, ECS 분해 템플릿, 밸런스 프레임워크)

#### 15. Phase 2D: 스테이지 & 난이도 (`5eae6f1`)
- StageManager 상태머신 (wave_intro → spawning → clearing → stage_clear → next_stage)
- 난이도 스케일링 (적 수/HP/이속/탄속 공식, 스테이지별 증가)
- entityFactory: difficulty 파라미터 지원
- lifespanSystem: 디스폰 전 깜빡임 효과 (2초 전부터)
- topHud: "Stage N Wave M/5" 표시

#### 16. 2D-fix: 데이터 드리븐 스테이지 + 4방향 스폰 (`59f923e`)
- **STAGE_DEFS 테이블**: 스테이지 1~5 수동 디자인 (웨이브수/스폰방향/적타입)
- **_getStageConfig()**: 정의된 스테이지는 테이블, 6+ 스테이지는 공식 자동 생성
- **4방향 스폰**: top/left/right/bottom 확률 기반 _pickSpawnDirection()
- **_getSpawnPosition()**: 방향별 카메라 밖 1+ unit 스폰 + 월드 바운드 clamp
- **_pickEnemyType()**: bottom 스폰 시 drift AI(basic/wave) 제외
- **doc/07_STAGE_DESIGN_GUIDE.md**: 스테이지 설계 방향 + 밸런스 레퍼런스

---

### 주요 설계 결정 (후반)

| 결정 | 선택 | 이유 |
|------|------|------|
| 웨이브 클리어 방식 | kill-all (전멸) | 실력 기반, 긴장감 유지 |
| 월드 크기 | 20×30 | 카메라 뷰(~4.5×10) 대비 적절한 긴장감 |
| 스테이지 확장 | 수동(1~5) + 자동(6+) | 디자인된 경험 + 무한 플레이 양립 |
| 스폰 방향 진행 | 위만 → 점진적 4방향 | Stage 1 입문 → Stage 5+ 하드코어 |

---

### Day 2 종료 시점 현황

| 항목 | 수치 |
|------|------|
| 총 커밋 | 20개 |
| 컴포넌트 | 16종 |
| 시스템 | 14종 + StageManager |
| 적 타입 | 4종 (basic, spiral, aimed, wave) |
| 탄막 패턴 | 4종 (circle, spiral, aimed, wave) |
| 불릿 풀 | 2000개 사전할당 |
| 월드 크기 | 20×30 (center 0,0) |
| 문서 | 8개 (00~07) |
| 완료 Phase | 0, 1, 2A, 2B, 2C, 2D |
